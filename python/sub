#!/usr/bin/env python

import sys

from veles.schema import nodeid
from veles.scli import client

client = client.create_client(sys.argv[1])

obj = nodeid.NodeID(sys.argv[2])

import pprint
for x in client.get_sub(obj):
	pprint.pprint(x)
