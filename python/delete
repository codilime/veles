#!/usr/bin/env python

import sys

from veles.schema import nodeid
from veles.scli import client

client = client.create_client(sys.argv[1])

objs = [nodeid.NodeID(x) for x in sys.argv[2:]]

client.delete(objs)
print('DELETED')
