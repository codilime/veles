#!/usr/bin/env python

import sys

from veles.schema import nodeid
from veles.scli import client

client = client.create_client(sys.argv[1])

objs = [nodeid.NodeID.from_hex(x) for x in sys.argv[2:]]

client.delete(objs)
print('DELETED')
