#!/usr/bin/env python

import sys
from scli import client
from binascii import a2b_hex

from common import base

client = client.create_client(sys.argv[1])

objs = [base.ObjectID(a2b_hex(x)) for x in sys.argv[2:]]

client.delete(objs)
print('DELETED')
